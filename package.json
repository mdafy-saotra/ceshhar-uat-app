{
  "name": "ceshhar-uat-app",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^5.0.0"
  }
}
```

### Step 2 — Make sure your `src/` folder has exactly these 2 files:
- `src/App.jsx` ← the React code
- `src/main.jsx` ← the entry point

### Step 3 — Add a `.env` file in the root to disable ESLint errors:
```
DISABLE_ESLINT_PLUGIN=true